apiVersion: v1
kind: Service
metadata:
  name: pembayaran-service
  labels:
    app: pembayaran-service
spec:
  type: ClusterIP
  selector:
    app: pembayaran-service
  ports:
    - name: http
      port: 8085
      targetPort: 8085
